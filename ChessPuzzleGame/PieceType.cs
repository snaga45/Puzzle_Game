namespace ChessPuzzleGame  // Make sure this matches your project namespace
{
    // Public enum accessible to all classes
    public enum PieceType
    {
        Empty,
        King,
        Bishop,
        Rook
    }
}